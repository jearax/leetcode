packages:
  - "apps/*"
  - "packages/*"

# Approve postinstall scripts for these packages. Required for vitest
# (uses esbuild native binary) and turbo prune (uses unrs-resolver).
allowBuilds:
  esbuild: true
  unrs-resolver: true